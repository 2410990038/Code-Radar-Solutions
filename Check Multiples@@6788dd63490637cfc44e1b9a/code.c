#include <stdio.h>


int main() {
    int x,y;
    scanf("%d",&x,&y);
    if (b!=0 && a%b==0) {
        printf("Yes");
    } else  if (b==0) {
        printf("Yes");
    } else {
        printf("No");
    }

    return 0;
}